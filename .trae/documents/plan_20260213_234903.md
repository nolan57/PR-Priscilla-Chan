## Fix Plan for `pro_singer_separator.py`

### Issue: MDX-C models receive spectrogram input instead of raw waveform

**Location:** `separate_vocals()` method in `UVRSeparator` class (around line 835)

**Change:** Add MDX-C model handling after audio normalization (after line 856):

```python
# MDX-C models process waveform directly
if self.is_mdx_c:
    if progress_callback:
        progress_callback("Running MDX-C inference...")
    
    with torch.no_grad():
        waveform_tensor = torch.from_numpy(waveform).to(self.device).float()
        output = self.model(waveform_tensor)
        
        if isinstance(output, tuple):
            output = output[0]
        
        # MDX-C outputs waveform directly
        result = output.squeeze(0).cpu().numpy()
        
        # Return mono for compatibility
        if result.ndim > 1:
            result = np.mean(result, axis=0)
        
        return result

# Standard processing for non-MDX-C models continues below...
```

This will:
1. Check if the loaded model is MDX-C type (`self.is_mdx_c`)
2. If True, pass raw waveform tensor directly to the model
3. If False, continue with the existing spectrogram-based approach for VR/standard MDX models