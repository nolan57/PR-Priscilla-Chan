修改 `/Users/lpcw/Documents/PCS/ultimatevocalremovergui/demucs/states.py` 第46行：

将：
```python
package = torch.load(path, 'cpu')
```

改为：
```python
package = torch.load(path, 'cpu', weights_only=False)
```

这是解决 PyTorch 2.6 中 `torch.load` 默认参数变化导致 Demucs 模型加载失败的最小改动方案。